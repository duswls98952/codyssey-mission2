# Troubleshooting

- 첫 `loom task run`은 작업트리가 dirty라 실패했다.
- 문서/증빙/Loom metadata를 먼저 커밋하고, `main.py` 및 이번 Task 범위 밖 자료를 stash에 보관한 뒤 다시 실행했다.
- 브랜치 lock 생성 권한 문제는 승인된 실행으로 `loom task run`을 다시 수행해 해결했다.

## DONE Guardrail

아래 필수 작업 기록이 부족해 `DONE` 대신 `REVIEW_REQUIRED`로 전환했습니다.

- validation evidence

Next action: 완료 계약을 증명하는 기록이 부족하거나 미해결 요청이 있어 검토가 필요합니다. result/decision/troubleshooting/log/event/artifact와 검증 근거를 보강하거나 요청을 해결한 뒤 다시 완료 처리하세요.
